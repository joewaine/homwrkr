=form_for @assignment, :url => assignments_path, :remote => true do |f|
  = f.label :name
  = f.text_field :name
  = f.label :group_size
  = f.number_field :group_size
  = f.submit 'Save'
  = link_to_function('Cancel', 'cancel_form()')